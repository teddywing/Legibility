{
	"manifest_version": 2,
	"name": "Legibility",
	"description": "Apply custom CSS to web pages.",
	"version": "0.0.1",

	"background": {
		"scripts": ["background.js"]
	},

	"permissions": [
		"activeTab"
	]
}
